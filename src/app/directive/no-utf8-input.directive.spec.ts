import { NoUtf8InputDirective } from './no-utf8-input.directive';

describe('NoUtf8InputDirective', () => {
  it('should create an instance', () => {
    const directive = new NoUtf8InputDirective();
    expect(directive).toBeTruthy();
  });
});
